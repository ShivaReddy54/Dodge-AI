{
  "name": "o2c-explorer",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install",
    "import-data": "node api/import.mjs",
    "dev": "concurrently \"cd frontend && npm run dev\" \"node api/index.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
